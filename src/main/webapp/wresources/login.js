/* 
 * 实现ajax login 避免chrome等浏览器的bug影响登录
 */
var wzqrserver = wzqrserver || {};

wzqrserver.login = function(form){
    console.log($('form'));
//    $.post('login',{},function(data, textStatus, jqXHR){});
};


